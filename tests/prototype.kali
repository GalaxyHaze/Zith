public:

enum Color {
    blue = 0,
    red = 1,
    green = 2,
    black = 3,
    gray = 4
}

struct Point{
    public:
    x: u32;
    y: u32;
}

let miranha = "posição da aranha";

noreturn fn add( num1: lend long, num2: long ){
    marker sample(x: u32){

    }

   if true
        entry goto sample(5);
   else
        goto scene level1;

}


fn main( agrc: u32, argv: string ){

    let num1: u32 = 50;
    {
        let num2: u32 = 24;
    }


}