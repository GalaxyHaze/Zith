---
id: cli-run
title: zithc run
section: CLI Reference
output: cli/D-run.html
aliases: cli/D-run.html
kind: editorial
---
# `zithc run`

Compile and execute a project or source target in one command.

```bash
zithc run
zithc run src/main.zith
```

Use `run` for a local development loop. Use [build](doc:cli-build) when you need the compiled artifact without execution.
