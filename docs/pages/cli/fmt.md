---
id: cli-fmt
title: zithc fmt
section: CLI Reference
output: cli/D-fmt.html
aliases: cli/D-fmt.html
kind: editorial
---
# `zithc fmt`

Format a source file or directory using Zith formatting conventions.

```bash
zithc fmt src
zithc fmt --check src
zithc fmt -i src
```

`--check` reports whether formatting changes are needed. `-i` writes the formatted source in place.
